{
  "name": "@barkan/mcp",
  "version": "0.1.0",
  "description": "Barkan MCP stdio bridge and pairing CLI",
  "type": "module",
  "private": false,
  "bin": {
    "barkan-mcp": "dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "mongodb": "^6.9.0",
    "mongodb-memory-server": "^11.2.0",
    "vitest": "^4.1.5"
  }
}
